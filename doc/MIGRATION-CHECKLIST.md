# 迁移清单（MV3 重启）

## 使用方式
- 完成后将条目标记为：`[x]`。
- 在每个章节下方记录简短备注（日期、结果、阻塞点）。
- 建议按章节拆分为小型 PR 或提交。

## P0 - 运行稳定性
- [ ] Chrome 能从 `dist/` 成功加载未打包扩展，且无清单错误。
- [ ] Service worker 启动时无运行时异常。
- [ ] 浏览器重启后扩展仍可正常工作（service worker 可重新激活）。
- [ ] Popup 能正常打开并刷新状态（不会卡在 loading）。
- [ ] 在普通网页上，激活/停用操作可正常执行。
- [ ] 激活后，“Open Info Panel” 操作可正常执行。

### 备注
- 日期：
- 结果：
- 阻塞点：

---

## P0 - 核心行为冒烟测试
- [ ] 在 `https://` 页面可注入内容脚本。
- [ ] 在 `http://` 页面可注入内容脚本。
- [ ] 在受限页面（`chrome://`、扩展页）不会注入内容脚本。
- [ ] 嵌入式库检测仍可在需要时禁用扩展。
- [ ] 浏览器按钮图标/标题/徽章状态变化正确。
- [ ] 右键菜单项可显示，并能执行预期动作。

### 备注
- 日期：
- 结果：
- 阻塞点：

---

## P1 - 认证与会话
- [ ] Auth0 登录流程端到端可用。
- [ ] 登出流程端到端可用。
- [ ] 会话检测可用（有效 token 路径）。
- [ ] 会话过期路径可被正确处理。
- [ ] 用户资料请求可返回预期字段。
- [ ] 用户数据请求路径仍可正常工作。

### 备注
- 日期：
- 结果：
- 阻塞点：

---

## P1 - 跨浏览器方案
- [ ] 确定目标策略：
- [ ] `方案 A`：Chrome + Firefox 均使用 MV3。
- [ ] `方案 B`：Chrome 使用 MV3，Firefox 使用兼容拆分清单。
- [ ] 为不同浏览器定义产物名称与输出目录。
- [ ] 验证 Firefox 安装流程（临时加载或打包安装）。
- [ ] 验证浏览器之间的基线功能一致性。

### 备注
- 日期：
- 决策：
- 阻塞点：

---

## P2 - 构建与依赖现代化
- [ ] 保持 `package-lock.json` 现代化且可复现。
- [ ] 移除不再需要的临时兼容补丁。
- [ ] 审查弃用依赖并按影响分组：
- [ ] 仅构建工具链
- [ ] 运行时依赖
- [ ] 安全关键项
- [ ] 采用小批次升级，每批后执行冒烟测试。
- [ ] 减少安装告警，并记录不可避免的告警。

### 备注
- 日期：
- 已升级包：
- 回归问题：

---

## P2 - 代码库清理
- [ ] 保持后台代码 worker-safe（不依赖 `window` 假设）。
- [ ] 保持消息模型与重 UI 包解耦。
- [ ] 为 popup/background 消息链路增加防御性错误日志。
- [ ] 迁移稳定后清理仅 MV2 使用的死分支。
- [ ] 为后台消息命令增加轻量级集成测试。

### 备注
- 日期：
- 结果：
- 阻塞点：

---

## P3 - 发布准备
- [ ] 在 `README.md` 更新当前安装/测试流程。
- [ ] 在 `BUILD-FF-CHROME.md` 更新最终浏览器构建步骤。
- [ ] 在 `DEVELOPMENT.md` 更新迁移架构说明。
- [ ] 为 MV3 迁移阶段准备 changelog 条目。
- [ ] 生成内部候选版本并执行最终冒烟测试。

### 备注
- 日期：
- RC 版本：
- 最终阻塞点：
